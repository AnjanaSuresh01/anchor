.venv/
venv/
__pycache__/
*.py[cod]
.env
data/raw/
data/qdrant/
data/*.jsonl
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.DS_Store
# Everything under data/ is generated by the ingest, index, resolve and graph
# steps, and every one of those recreates the directory it needs. Listing the
# artefacts individually missed data/graph, which Kuzu writes as a single FILE
# rather than a directory, so a 12MB binary database was committed.
data/
logs/
